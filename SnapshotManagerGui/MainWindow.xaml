<Window x:Class="SnapshotManagerGui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SnapshotManagerGui"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <GroupBox x:Name="connectionsGroupBox" Header="Connections" Margin="10"/>
            <GroupBox x:Name="databasesGroupBox" Header="Databases
" Margin="10" Grid.Row="1"/>
        </Grid>
        <GroupBox x:Name="snapshotsGroupBox" Grid.Column="1" Header="Snapshots" Margin="10"/>
    </Grid>
</Window>
